using NUnit.Framework;
 
using System;
using Moq;
  
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace WhyInterfaceUnitTest
{
    [TestClass]
    public class Tests
    {
        
        [TestMethod]
        public void TestMethod1()
        {
        //    Mock<IcheckEmployee> chk = new Mock<IcheckEmployee>();
        //    chk.Setup(x => x.checkEmp()).Returns(true);

        //    processEmployee obje = new processEmployee();
        //    Assert.AreEqual(obje.insertEmployee(chk.Object), true);
        }

    }
}